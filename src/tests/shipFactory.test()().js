// const battleship = require('../dependencies/shipFactory.js');

// test('ensures ship is created at appropriate length', () => {
//   expect(battleship.ShipLength).toBe(4);
// });

// test('ensures ship name is created', () => {
//   expect(battleship.ShipName).toBe("battleship");
// });

// test('ensures ship can take hit', () => {
//   expect(battleship.hit(1)).toStrictEqual([1,0,1,1]);
// });

// test('ensures ship can take hit', () => {
//   expect(battleship.hit(0)).toStrictEqual([0,0,1,1]);
// });

// test('ensures ship can take hit', () => {
//   expect(battleship.hit(2)).toStrictEqual([0,0,0,1]);
// });
// test('ensures ship can be sunk', () => {
//   expect(battleship.isSunk()).toBe(false);
// });

// test('ensures ship can take hit', () => {
//   expect(battleship.hit(3)).toStrictEqual([0,0,0,0]);
// });

// test('ensures ship can be sunk', () => {
//   expect(battleship.isSunk()).toBe(true);
// });

